# React + TypeScript + Vite + @tma.js

To run the project follow these steps:

- Install all dependencies by running

```bash
npm i
```

- After all dependencies installed run the project and follow the url in console:

```bash
npm run dev
```

**_You may use your package manager_**
